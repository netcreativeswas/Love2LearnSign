<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Splash screen pour Android 12+ en mode sombre -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowSplashScreenBackground">@android:color/white</item>
        <item name="android:windowSplashScreenAnimatedIcon">@null</item>
        <item name="android:windowSplashScreenBrandingImage">@null</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@android:color/white</item>
    </style>
    
    <!-- Votre thème DynamicColors Dark -->
    <style name="NormalTheme" parent="@style/Theme.lovetolearnsign">
        <item name="android:windowBackground">?attr/colorBackground</item>
    </style>
</resources>
