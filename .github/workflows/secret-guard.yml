name: Secret guard

on:
  push:
    branches: [main]
  pull_request: {}

jobs:
  secret-guard:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run secret guard
        run: |
          chmod +x scripts/secret_guard.sh
          scripts/secret_guard.sh


